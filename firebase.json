{
  "hosting": {
    "public": "public", // Replace with your public directory
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "function": "app" // Assuming "app" is the Firebase function serving index.html
      }
    ]
  }
}
